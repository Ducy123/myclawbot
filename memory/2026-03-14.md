# 2026-03-14

- Người dùng quyết định giữ lại context/memory hiện tại, không xóa bớt workspace memory nữa.
- Đã import và cài skill `self-improvement` từ file zip người dùng gửi; đã sửa frontmatter `SKILL.md` cho đúng spec OpenClaw, cài vào `/root/.openclaw/skills/self-improvement`, khởi tạo `.learnings/` trong workspace, và mirror skill vào repo workspace.
- Đã mirror phần portable của Ducy + Elly + skill/docs vận hành vào repo workspace chính theo cấu trúc sạch (`profiles/elly/`, `skills/self-improvement/`, `REPO_LAYOUT.md`) và tránh kéo theo secrets/session/runtime state.
- Người dùng đã tạo repo GitHub `https://github.com/Ducy123/myclawbot.git`, chọn policy mức B về lâu dài: commit local thường xuyên, push khi người dùng yêu cầu.
- Đã tạo SSH key GitHub trên VPS chính, người dùng đã add public key vào GitHub và xác nhận authenticated; sau đó đã set remote và push repo workspace chính lên GitHub thành công.
- Đã add thêm skill `find-skills` từ file zip người dùng gửi, cài vào `/root/.openclaw/skills/find-skills`, mirror vào repo tại `skills/find-skills/`, và push lên GitHub.
- Đã add thêm skill `gog` (Google Workspace CLI) từ file zip người dùng gửi, dọn frontmatter `SKILL.md` cho đúng spec OpenClaw, cài vào `/root/.openclaw/skills/gog`, mirror vào repo tại `skills/gog/`, và chuẩn bị dùng cho Gmail/Calendar/Drive/Sheets/Docs workflows.
- Đã add thêm skill `proactive-agent` từ file zip người dùng gửi, dọn frontmatter `SKILL.md` cho đúng spec OpenClaw, loại bỏ file backup/draft thừa trong gói, cài vào `/root/.openclaw/skills/proactive-agent`, mirror vào repo tại `skills/proactive-agent/`, và push lên GitHub.
- Đã add thêm skill `humanizer` từ file zip người dùng gửi, dọn frontmatter `SKILL.md` cho đúng spec OpenClaw, loại bỏ `README.md` thừa trong gói import, cài vào `/root/.openclaw/skills/humanizer`, mirror vào repo tại `skills/humanizer/`, và push lên GitHub.
- Đã add thêm skill `nano-banana-pro` từ file zip người dùng gửi, cài vào `/root/.openclaw/skills/nano-banana-pro`, mirror vào repo tại `skills/nano-banana-pro/`, và sửa các lệnh ví dụ trong `SKILL.md` để dùng đúng path OpenClaw thay vì path `~/.codex/skills/...`.
- Đã add thêm skill `api-gateway` từ file zip người dùng gửi, dọn frontmatter `SKILL.md` cho đúng spec OpenClaw, cài vào `/root/.openclaw/skills/api-gateway`, mirror vào repo tại `skills/api-gateway/`, và push lên GitHub.
