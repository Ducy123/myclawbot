# 2026-03-12

- Người dùng đặt tên cho trợ lý là "Ducy" trong cuộc trò chuyện đầu tiên trên Telegram.
- Đã mở cấu hình Telegram group của OpenClaw sang `groupPolicy: "open"` và `groups["*"].requireMention: true` để bot được phép ở mọi group nhưng chỉ trả lời khi bị tag/mention.
