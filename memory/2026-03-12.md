# 2026-03-12

- Người dùng đặt tên cho trợ lý là "Ducy" trong cuộc trò chuyện đầu tiên trên Telegram.
- Đã mở cấu hình Telegram group của OpenClaw sang `groupPolicy: "open"` và `groups["*"].requireMention: true` để bot được phép ở mọi group nhưng chỉ trả lời khi bị tag/mention.
- Đã bật `linger` cho root và xác nhận OpenClaw gateway chạy nền độc lập với phiên SSH; bot vẫn online khi SSH tắt.
- Đã tạo agent `elly` với workspace riêng và bind vào Telegram account `elly` trên cùng Gateway.
- Đã cài Chromium trên VPS để chuẩn bị browser session cho Elly; cần bật `browser.noSandbox: true` vì gateway đang chạy dưới root trên Ubuntu headless.
- Sau đó đã chuyển sang Google Chrome non-snap trên VPS để tránh lỗi quyền/lock do Chromium snap gây ra cho browser profile của OpenClaw.
- Đã dựng VPS phụ thành browser/file worker cho Elly: SSH key từ VPS chính sang worker đã hoạt động, browser-worker.service trên máy phụ đã ổn định, và đã chuẩn hóa lệnh điều khiển worker trong `WORKER.md` + `bin/elly-*`.
- Đã mirror phần portable của Elly workspace + imported skills + docs vận hành quan trọng vào repo workspace chính để sau này backup/pull lại dễ hơn mà không kéo theo secrets/session sống.
