# 2026-03-15

- Ducy yêu cầu agent reload từ disk, refresh memory, lưu mọi update mới, commit và push repo workspace lên GitHub ngay; đồng thời tự động hóa workflow này chạy mỗi ngày lúc 08:00 UTC.
- Đã rà soát repo local trước khi push: phát hiện thay đổi trong `.learnings/ERRORS.md` và workspace `embedding/`; thêm ignore cho virtualenv để tránh đẩy `embedding/.venv/` lên GitHub.
